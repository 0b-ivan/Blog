version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  kernel-notes:
    lang: dockerfile
    handler: ./function-blog
    image: ghcr.io/your-user/kernel-notes-function:latest
    environment:
      write_timeout: 20s
      read_timeout: 20s
      exec_timeout: 20s
