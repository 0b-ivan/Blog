name: CI

on:
  pull_request:
  push:
    branches:
      - "**"

jobs:
  validate-and-smoke-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Validate post frontmatter metadata
        run: npm run posts:validate-meta

      - name: Lint
        run: npm run lint

      - name: Unit and API tests with coverage
        run: npm run test:coverage

      - name: Validate Docker Compose
        run: docker compose config

      - name: Validate production Docker Compose
        env:
          BLOG_IMAGE: ghcr.io/0b-ivan/kernel-notes-blog:ci
        run: docker compose -f docker-compose.prod.yml config

      - name: Validate Obsidian LiveSync Docker Compose
        env:
          COUCHDB_USER: obsidian-ci
          COUCHDB_PASSWORD: ci-placeholder-password
          COUCHDB_DATABASE: kernelnotes
          CF_TUNNEL_TOKEN: ci-placeholder-token
          OBSIDIAN_VAULT_PATH: ../../posts
        run: |
          docker compose -f ops/obsidian-livesync/docker-compose.yml config
          docker compose -f ops/obsidian-livesync/docker-compose.yml --profile cloudflare config
          docker compose -f ops/obsidian-livesync/docker-compose.yml --profile headless config
          docker compose -f ops/obsidian-livesync/docker-compose.yml --profile cloudflare --profile headless config

      - name: Build local blog image
        run: docker build -t kernel-notes-blog:ci .

      - name: Start stack
        run: docker compose up -d --build

      - name: Wait for health endpoint
        run: |
          for i in {1..20}; do
            if curl -fsS http://127.0.0.1:8080/healthz > /dev/null; then
              echo "Service is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "Service did not become healthy in time"
          docker compose ps
          exit 1

      - name: Shutdown stack
        if: always()
        run: docker compose down -v

  security-trivy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Build blog image for scan
        run: docker build -t kernel-notes-blog:ci .

      - name: Trivy filesystem scan (report)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          format: table
          ignore-unfixed: true
          vuln-type: os,library
          severity: HIGH,CRITICAL
          exit-code: '0'

      - name: Trivy image scan (gate on CRITICAL)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: kernel-notes-blog:ci
          format: table
          ignore-unfixed: true
          vuln-type: os,library
          severity: HIGH,CRITICAL
          exit-code: '0'

      - name: Trivy image scan (strict gate on CRITICAL)
        if: ${{ vars.TRIVY_STRICT == 'true' }}
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: kernel-notes-blog:ci
          format: table
          ignore-unfixed: true
          vuln-type: os,library
          severity: CRITICAL
          exit-code: '1'
