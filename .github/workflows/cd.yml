name: CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  packages: write

concurrency:
  group: production-deploy
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  BLOG_IMAGE_NAME: kernel-notes-blog

jobs:
  verify-main-origin:
    runs-on: ubuntu-latest

    steps:
      - name: Require merged PR for main deployments
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          PR_COUNT=$(gh api \
            -H "Accept: application/vnd.github+json" \
            "/repos/${REPOSITORY}/commits/${COMMIT_SHA}/pulls" \
            --jq '[.[] | select(.merged_at != null and .base.ref == "main")] | length')

          if [ "$PR_COUNT" -lt 1 ]; then
            echo "Refusing deployment: main commit is not associated with a merged pull request."
            exit 1
          fi

          echo "Main commit is associated with a merged pull request."

  build-and-push:
    runs-on: ubuntu-latest
    needs: verify-main-origin
    outputs:
      blog_image: ${{ steps.meta.outputs.blog_image }}
      blog_version: ${{ steps.meta.outputs.blog_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set image name and deployment version
        id: meta
        run: |
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          BASE_VERSION=$(node -p "require('./package.json').version")
          VERSION_PREFIX="${BASE_VERSION%.*}"
          BLOG_VERSION="${VERSION_PREFIX}.${GITHUB_RUN_NUMBER}"

          echo "owner_lower=${OWNER_LOWER}" >> "$GITHUB_OUTPUT"
          echo "blog_version=${BLOG_VERSION}" >> "$GITHUB_OUTPUT"
          echo "blog_image=${{ env.REGISTRY }}/${OWNER_LOWER}/${{ env.BLOG_IMAGE_NAME }}:${{ github.sha }}" >> "$GITHUB_OUTPUT"
          echo "Deployment version: ${BLOG_VERSION}"

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push blog image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            BUILD_VERSION=${{ steps.meta.outputs.blog_version }}
          tags: |
            ${{ steps.meta.outputs.blog_image }}
            ${{ env.REGISTRY }}/${{ steps.meta.outputs.owner_lower }}/${{ env.BLOG_IMAGE_NAME }}:${{ steps.meta.outputs.blog_version }}
            ${{ env.REGISTRY }}/${{ steps.meta.outputs.owner_lower }}/${{ env.BLOG_IMAGE_NAME }}:latest

  deploy-to-hetzner:
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Validate Hetzner deployment secrets
        env:
          HETZNER_HOST: ${{ secrets.HETZNER_HOST }}
          HETZNER_USER: ${{ secrets.HETZNER_USER }}
          HETZNER_SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
          HETZNER_KNOWN_HOSTS: ${{ secrets.HETZNER_KNOWN_HOSTS }}
        run: |
          test -n "$HETZNER_HOST" || { echo "HETZNER_HOST is missing"; exit 1; }
          test -n "$HETZNER_USER" || { echo "HETZNER_USER is missing (recommended: blog-deploy)"; exit 1; }
          test -n "$HETZNER_SSH_KEY" || { echo "HETZNER_SSH_KEY is missing"; exit 1; }
          test -n "$HETZNER_KNOWN_HOSTS" || { echo "HETZNER_KNOWN_HOSTS is missing"; exit 1; }

      - name: Configure SSH
        env:
          HETZNER_SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
          HETZNER_KNOWN_HOSTS: ${{ secrets.HETZNER_KNOWN_HOSTS }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$HETZNER_SSH_KEY" > ~/.ssh/hetzner_deploy
          chmod 600 ~/.ssh/hetzner_deploy
          printf '%s\n' "$HETZNER_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Copy production Compose file
        env:
          HETZNER_HOST: ${{ secrets.HETZNER_HOST }}
          HETZNER_USER: ${{ secrets.HETZNER_USER }}
          HETZNER_PORT: ${{ secrets.HETZNER_PORT }}
        run: |
          PORT="${HETZNER_PORT:-22}"
          scp \
            -i ~/.ssh/hetzner_deploy \
            -P "$PORT" \
            -o BatchMode=yes \
            -o StrictHostKeyChecking=yes \
            docker-compose.prod.yml \
            "${HETZNER_USER}@${HETZNER_HOST}:/tmp/kernel-notes-compose.yml"

      - name: Login Hetzner host to GHCR
        env:
          HETZNER_HOST: ${{ secrets.HETZNER_HOST }}
          HETZNER_USER: ${{ secrets.HETZNER_USER }}
          HETZNER_PORT: ${{ secrets.HETZNER_PORT }}
          GHCR_TOKEN: ${{ github.token }}
          GHCR_USER: ${{ github.actor }}
        run: |
          PORT="${HETZNER_PORT:-22}"
          printf '%s' "$GHCR_TOKEN" | ssh \
            -i ~/.ssh/hetzner_deploy \
            -p "$PORT" \
            -o BatchMode=yes \
            -o StrictHostKeyChecking=yes \
            "${HETZNER_USER}@${HETZNER_HOST}" \
            "docker login ghcr.io -u '${GHCR_USER}' --password-stdin"

      - name: Deploy exact main image
        env:
          HETZNER_HOST: ${{ secrets.HETZNER_HOST }}
          HETZNER_USER: ${{ secrets.HETZNER_USER }}
          HETZNER_PORT: ${{ secrets.HETZNER_PORT }}
          BLOG_IMAGE: ${{ needs.build-and-push.outputs.blog_image }}
        run: |
          PORT="${HETZNER_PORT:-22}"
          ssh \
            -i ~/.ssh/hetzner_deploy \
            -p "$PORT" \
            -o BatchMode=yes \
            -o StrictHostKeyChecking=yes \
            "${HETZNER_USER}@${HETZNER_HOST}" \
            bash -s -- "$BLOG_IMAGE" <<'REMOTE'
          set -euo pipefail

          BLOG_IMAGE="$1"
          DEPLOY_DIR="/opt/Blog"
          COMPOSE_FILE="${DEPLOY_DIR}/docker-compose.prod.yml"
          ENV_FILE="${DEPLOY_DIR}/.env.production"
          CONTAINER_NAME="kernel-notes-blog"
          EXPECTED_HOST_PORT="1888"

          mkdir -p "$DEPLOY_DIR"
          mv /tmp/kernel-notes-compose.yml "$COMPOSE_FILE"

          PREVIOUS_IMAGE="$(docker inspect -f '{{.Config.Image}}' "$CONTAINER_NAME" 2>/dev/null || true)"
          printf 'BLOG_IMAGE=%s\n' "$BLOG_IMAGE" > "$ENV_FILE"

          docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" pull blog
          docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" up -d --remove-orphans

          PUBLISHED="$(docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" port blog 8080 2>/dev/null || true)"
          HEALTH_PORT="${PUBLISHED##*:}"

          echo "Published blog port: ${PUBLISHED:-<none>}"

          healthy=false
          if [ "$HEALTH_PORT" = "$EXPECTED_HOST_PORT" ]; then
            for _ in $(seq 1 20); do
              if curl -fsS "http://127.0.0.1:${HEALTH_PORT}/healthz" >/dev/null; then
                healthy=true
                break
              fi
              sleep 2
            done
          else
            echo "Unexpected production port: expected ${EXPECTED_HOST_PORT}, got ${HEALTH_PORT:-none}"
          fi

          if [ "$healthy" != "true" ]; then
            echo "Deployment health check failed"
            docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" ps || true
            docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" logs --tail=100 blog || true

            if [ -n "$PREVIOUS_IMAGE" ]; then
              echo "Rolling back to $PREVIOUS_IMAGE"
              printf 'BLOG_IMAGE=%s\n' "$PREVIOUS_IMAGE" > "$ENV_FILE"
              docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" up -d --remove-orphans
            fi

            exit 1
          fi

          echo "Deployed $BLOG_IMAGE successfully on host port $HEALTH_PORT"
          docker image prune -f >/dev/null
          REMOTE
